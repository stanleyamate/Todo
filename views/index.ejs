<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head'); %>
</head>
<body>
    <div class="container">
        <h1>Todo App </h1>
        <form action ="/addtask" method="POST">
            <input type="text" name="newtask" placeholder="add new task" id="taskinput">        <button class="button btn-task"> Add Task </button>
            <h2>Tasks </h2>
            <% for( var i = 0; i < task.length; i++){ %>
                <ul>
                    <li><input type="checkbox" name="check" value="<%= task[i] %>" /> <%= task[i] %>  </li>
                </ul>
                <% } %>
                <button formaction="/removetask" type="submit" class="button btn-rm">Delete</button>
                <button formaction="/edittask" class="button btn-list">Edit</button>
            </form>
            <h2> Completed task </h2>
            <% for(var i = 0; i < complete.length; i++){ %>
                <ul>
                    <li><%= complete[i] %> </li>
                </ul>
                <% } %>
                
            </div>
            
        </body> 
        </html>
        
        